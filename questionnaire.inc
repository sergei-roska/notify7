<?php

/**
 * Returns HTML for a alertify.
 *
 * @param $variables
 *   An associative array.
 *
 * @ingroup themeable
 */
function theme_questionnaire_alert_block($variables) {
  $output = '<div id="questionnaire--notify" class="notification-wrapper"></div>';

  return $output;
}